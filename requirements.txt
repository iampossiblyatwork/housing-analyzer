# Pinned for reproducible Docker builds (Render builds from this file).
# These are the versions the app is tested against — bump deliberately.
flask==3.1.3
requests==2.33.1
python-dotenv==1.2.2
twilio==9.10.9
gunicorn==26.0.0
